PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=showroom_db
DB_USER=postgres
DB_PASSWORD=mubah@8
# Optional: set instead of the 5 DB_* vars above (e.g. for hosted Postgres)
# DATABASE_URL=postgres://user:password@host:5432/dbname
# DB_SSL=true

JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=1d

CLIENT_URL=http://localhost:5173
UPLOAD_DIR=uploads
