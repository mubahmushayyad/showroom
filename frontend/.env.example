# Real backend (Express + Sequelize + PostgreSQL, see /backend):
VITE_API_URL=http://localhost:5000/api

# The old mock json-server API in /server still works as a drop-in
# replacement for local UI-only work if you don't want to run
# PostgreSQL: `npm run mock-api` (serves the same REST contract on
# port 4000) — but note it only ever implemented the Users endpoints,
# not Cars/Suppliers/Customers/Applications.
# VITE_API_URL=http://localhost:4000/api
