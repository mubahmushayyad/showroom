import {Button,Paper,Stack,Switch,FormControlLabel,Typography,Alert} from '@mui/material';import PageHeader from '../../components/common/PageHeader';import {useApp} from '../../context/AppContext';export default function Settings(){const {settings,saveSettings,resetDemo}=useApp();return <><PageHeader title="Settings" subtitle="Frontend preferences and demo data controls"/><Paper sx={{p:3,maxWidth:700}}><Stack spacing={2}><FormControlLabel control={<Switch checked={!!settings.darkMode} onChange={e=>saveSettings({...settings,darkMode:e.target.checked})}/>} label="Dark mode"/><Alert severity="info">All business data is stored in LocalStorage as required by the assignment. Resetting demo data restores seeded cars, suppliers, users and customers.</Alert><Button color="error" variant="outlined" onClick={()=>{if(confirm('Reset all demo data?'))resetDemo()}}>Reset Demo Data</Button></Stack></Paper></>}
