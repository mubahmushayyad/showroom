import {Paper,List,ListItem,ListItemText,Container} from '@mui/material';import PageHeader from '../../components/common/PageHeader';import {useAuth} from '../../context/AuthContext';import {useApp} from '../../context/AppContext';export default function Notifications(){const {user}=useAuth();const {notifications,markRead}=useApp();const mine=notifications.filter(n=>!n.userId||n.userId===user.id);return <Container maxWidth="lg" sx={{py:{xs:3,md:5}}}><PageHeader title="Notifications" subtitle="Local simulated notification center"/><Paper><List>{mine.map(n=><ListItem key={n.id} button onClick={()=>markRead(n.id)}><ListItemText primary={n.title} secondary={n.message}/></ListItem>)}</List></Paper></Container>}
