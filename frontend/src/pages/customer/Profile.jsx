import {useState} from 'react';import {Paper,Stack,TextField,Button,Container} from '@mui/material';import PageHeader from '../../components/common/PageHeader';import {useAuth} from '../../context/AuthContext';import {useApp} from '../../context/AppContext';export default function Profile(){const {user}=useAuth();const {customers,saveCustomer}=useApp();const base=customers.find(c=>c.userId===user.id)||{id:'CUS-'+user.id,userId:user.id,name:user.name,email:user.email};const [f,setF]=useState(base);return <Container maxWidth="lg" sx={{py:{xs:3,md:5}}}><PageHeader title="My Profile" subtitle="Your customer information"/><Paper sx={{p:3,maxWidth:700}}><Stack spacing={2}>{['name','email','phone','cnic','address','city'].map(k=><TextField key={k} label={k} value={f[k]||''} onChange={e=>setF({...f,[k]:e.target.value})}/>) }<Button variant="contained" onClick={()=>saveCustomer(f,user.name)}>Save Profile</Button></Stack></Paper></Container>}
